#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0); ios_base::sync_with_stdio(0);
    string p;
    cin >> p;
    int l;
    cin >> l;
    cout << (p.size() >= l ? "Yes\n" : "No\n");
}