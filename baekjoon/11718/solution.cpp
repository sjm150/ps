#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0); ios_base::sync_with_stdio(0);
    string s;
    while (!cin.eof()) {
        getline(cin, s);
        cout << s << '\n';
    }
}